# 如何在 MetaMask 中添加 Mantle Sepolia 测试网络

## 问题说明

由于 WalletConnect 的限制，移动端应用无法自动为 MetaMask 添加自定义网络。您需要在 MetaMask 中手动添加 Mantle Sepolia 测试网络。

## 解决方案：在 MetaMask 中手动添加网络

### 步骤 1: 打开 MetaMask 应用

在您的手机上打开 MetaMask 应用。

### 步骤 2: 进入网络设置

1. 点击左上角的 **三横线菜单** 图标
2. 选择 **设置 (Settings)**
3. 选择 **网络 (Networks)**
4. 点击 **添加网络 (Add Network)**

### 步骤 3: 选择添加自定义网络

- 如果看到网络列表，滚动到底部点击 **添加自定义网络 (Add a custom network)**
- 如果直接进入表单，跳过此步骤

### 步骤 4: 填写 Mantle Sepolia 网络信息

请精确填写以下信息：

| 字段 | 值 |
|------|-----|
| **网络名称 (Network Name)** | Mantle Sepolia Testnet |
| **RPC URL** | `https://rpc.sepolia.mantle.xyz` |
| **链 ID (Chain ID)** | `5003` |
| **货币符号 (Currency Symbol)** | `MNT` |
| **区块浏览器 URL (Block Explorer)** | `https://sepolia.mantlescan.xyz` |

### 步骤 5: 保存并切换

1. 点击 **添加 (Add)** 或 **保存 (Save)**
2. 确保当前网络已切换到 **Mantle Sepolia Testnet**
3. 您应该在顶部看到 "Mantle Sepolia Testnet" 的标识

### 步骤 6: 重新连接 ParkView 应用

1. 如果之前已连接，请先在 ParkView 中断开钱包连接
2. 重新连接钱包
3. 这次应该可以正常使用所有功能了！

## 验证网络是否正确

连接后，您应该看到：
- ✅ 网络名称: Mantle Sepolia Testnet
- ✅ Chain ID: 5003
- ✅ 可以正常执行交易（发布车位、租赁、删除等）

## 获取测试 MNT

如果您需要测试 MNT 代币：

1. 访问 Mantle Sepolia Faucet (如果有)
2. 或联系项目方获取测试代币

## 常见问题

### Q1: 为什么不能自动添加网络？

A: WalletConnect v2 在移动端通过 deep link 调用 MetaMask 时，`wallet_addEthereumChain` 方法可能无法触发 UI 提示。这是 MetaMask 和 WalletConnect 的已知限制。

### Q2: 我添加了网络但仍然失败？

A: 请确保：
1. Chain ID 必须是 `5003` (不是 0x138B)
2. RPC URL 完全正确: `https://rpc.sepolia.mantle.xyz`
3. 当前网络已切换到 Mantle Sepolia
4. 重新连接 ParkView 应用

### Q3: 可以使用其他网络吗？

A: 不可以。ParkView 的智能合约部署在 Mantle Sepolia 测试网络上，必须使用此网络才能与合约交互。

## 技术细节

- **为什么选择 optionalNamespaces?**
  - 使用 `optionalNamespaces` 而不是 `requiredNamespaces` 可以让 MetaMask 灵活选择支持的链
  - 连接后应用会检查 session 支持的链列表
  - 如果不包含 Mantle Sepolia，会提示用户手动切换

- **网络检查逻辑:**
  ```typescript
  const supportedChains = session.namespaces.eip155.chains || [];
  const mantleChainId = 'eip155:5003';
  if (!supportedChains.includes(mantleChainId)) {
    // 提示用户手动切换网络
  }
  ```

## 下一步

一旦在 MetaMask 中添加并切换到 Mantle Sepolia 网络，您就可以：

- ✅ 发布停车位 NFT
- ✅ 租赁停车位
- ✅ 更新停车位信息
- ✅ 删除停车位
- ✅ 终止租赁合约

所有区块链操作都将正常工作！🚀
